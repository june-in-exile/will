{
  "name": "circuits",
  "version": "1.0.0",
  "description": "1. Writing circuits.",
  "main": "index.js",
  "scripts": {
    "clean": "find . -maxdepth 1 -type f ! -name 'README.md' ! -name 'Makefile' ! -name '*.circom' ! -name 'package*.json' -delete && find . -maxdepth 1 -type d ! -name '.' -exec rm -rf {} +",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build" : "make all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  }
}
